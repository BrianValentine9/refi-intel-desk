# Pinned to versions resolved on Python 3.14 (2026-06).
# Re-pin with `pip freeze` if you bump the Python or a dependency.
streamlit==1.58.0
plotly==6.8.0
pandas==3.0.3
requests==2.34.2
python-dotenv==1.2.2
anthropic==0.84.0
pytest==9.0.3
